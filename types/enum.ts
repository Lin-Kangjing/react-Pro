
export enum BasicStatus {
  DISABLE,
  ENABLE
}
export enum StorageEnum {
  User = 'user',
  Token = 'token',
  Settings = 'settings',
  I18N = 'i18nextLng',
}

export enum PermissionType {
  CATALOGUE,
  MENU,
  BUTTON,
}
